#include <iostream>
#include <string>
#include "Scrappers.h"

using namespace std;

void Scrappers() {
    int choice;

    do{
        cout << "Welcome to Scrappers!\n Want to play a game? 1 for yes, 0 no.";
        cin >> choice;
    }while(choice == 1);
}